import {
  Create__entity__(pascalCase)DtoSchema
} from "@__module__(camelCase)/application/dto/__entity__(kebabCase)/create-__entity__(kebabCase).dto";
import { z } from 'zod';

export const Create__entity__(pascalCase)ReqDtoSchema = Create__entity__(pascalCase)DtoSchema.extend({});
export type Create__entity__(pascalCase)ReqDtoType = z.infer<typeof Create__entity__(pascalCase)ReqDtoSchema>;
