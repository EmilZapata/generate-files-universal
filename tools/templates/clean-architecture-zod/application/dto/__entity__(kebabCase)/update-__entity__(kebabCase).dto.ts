import { __entity__(pascalCase)Schema } from "@__module__(camelCase)/domain/interfaces/__entity__(kebabCase).interface";
import { z } from 'zod';

export const Update__entity__(pascalCase)Dto = __entity__(pascalCase)Schema.pick({ id: true });
export type Update__entity__(pascalCase)DtoType = z.infer<typeof Update__entity__(pascalCase)Dto>;

