import { __entity__(pascalCase)Schema } from "@__module__(camelCase)/domain/interfaces/__entity__(kebabCase).interface";
import { z } from 'zod';

export const Create__entity__(pascalCase)Dto = __entity__(pascalCase)Schema.pick({});
export type Create__entity__(pascalCase)DtoType = z.infer<typeof Create__entity__(pascalCase)Dto>;
